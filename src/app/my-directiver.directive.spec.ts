import { MyDirectiverDirective } from './my-directiver.directive';

describe('MyDirectiverDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDirectiverDirective();
    expect(directive).toBeTruthy();
  });
});
